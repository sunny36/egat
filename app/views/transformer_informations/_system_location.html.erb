<%= label :transformer_information, :system_location_id, "System Location" %>
<%= @system_locations = SystemLocation.find(:all, :order => 'value')
  f.collection_select(:system_location_id,  @system_locations, :id, 
                    :value, {:prompt => true}) %>        
