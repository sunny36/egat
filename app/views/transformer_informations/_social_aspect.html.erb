<%= label :transformer_information, :social_aspect_id,'Social Aspect' %>
<%= 
  @social_aspects = SocialAspect.find(:all, 
                                      :conditions => "value IS NOT NULL", 
                                      :order => 'score')
  f.collection_select(:social_aspect_id,  @social_aspects, :id, 
                      :value, {:prompt => true}) 
%>
